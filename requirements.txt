# Core dependencies (generated from pyproject.toml for Docker/compatibility)
fastapi
uvicorn[standard]
pymongo
python-jose[cryptography]
passlib[bcrypt]
apscheduler

# Note: Use 'uv pip compile pyproject.toml -o requirements.txt' or similar for locked versions
